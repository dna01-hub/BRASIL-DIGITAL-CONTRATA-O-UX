# =============================================================
# Variáveis de Ambiente — Brasil Digital Checkout
# =============================================================
# Copie este arquivo para .env e preencha os valores.
# NUNCA comite o .env real no repositório.
# =============================================================

# ------ Vercel (server-side apenas, sem prefixo VITE_) -------
# Configure em: Vercel → Project → Settings → Environment Variables

# Token de autenticação do backend SIGA (nunca exposto ao cliente)
API_TOKEN=seu-api-token-aqui

# URL base da API do backend
API_URL=https://homologacao-siga.brasildigital.net.br/api/v1

# Chave interna para proteger o endpoint /api/analyze-customer
# Gere uma chave aleatória forte (ex: openssl rand -hex 32)
INTERNAL_KEY=sua-chave-secreta-aqui

# ------ Client-side (prefixo VITE_, ficam no bundle JS) ------
# Configure também nas env vars da Vercel com o mesmo prefixo

# Mesma chave de INTERNAL_KEY — enviada no header x-internal-key
# Fica visível no bundle, mas protege contra chamadas diretas ao endpoint
VITE_INTERNAL_KEY=sua-chave-secreta-aqui
